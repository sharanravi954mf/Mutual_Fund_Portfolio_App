name: Documentation Quality Validation

on:
  push:
    branches: [ main, develop, 'release/*', 'feature/*', 'bugfix/*', 'hotfix/*' ]
  pull_request:
    branches: [ main, develop, 'release/*', 'feature/*', 'bugfix/*', 'hotfix/*' ]

jobs:
  validate-docs:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository code
      uses: actions/checkout@v4

    - name: Set up Python 3.10
      uses: actions/setup-python@v5
      with:
        python-level: '3.10'

    - name: Run Documentation Quality and Link Audit
      run: python3 .github/scripts/validate_docs.py
