# Architecture Decisions Record (ADR)

This folder contains Architecture Decision Records (ADRs) tracking structural design choices.

## Contents
* Format follows standard ADR (Title, Date, Status, Context, Decision, Consequences).
* Explains choices like state management selection (Provider), return rate metrics (XIRR), and custom painting calculations.
